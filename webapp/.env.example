# مفتاح Anthropic API — احصل عليه من https://platform.claude.com
ANTHROPIC_API_KEY=sk-ant-...

# النموذج المستخدم (اختياري)
ANTHROPIC_MODEL=claude-opus-4-8

# رابط قاعدة البيانات (اختياري — docker-compose يضبطه تلقائياً)
# DATABASE_URL=postgresql+psycopg2://tasks:tasks@localhost:5432/tasksdb
